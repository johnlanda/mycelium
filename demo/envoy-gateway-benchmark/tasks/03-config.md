Create a ClientTrafficPolicy YAML for Envoy Gateway v1.3.0 that: targets a Gateway "eg", enables HTTP/3, configures XFCC header handling (SanitizeSet mode), rejects headers with underscores (RejectRequest), disables X-RateLimit headers, enables TCP keepalive (60s idle, 30s interval, 3 probes), and enables early request header modification with "X-Request-Start" set to "%START_TIME%". Reference exact Go types and field names.
