Write Kubernetes YAML manifests for: (1) An Envoy Gateway Backend resource routing to api.example.com:443 via FQDN endpoints, (2) A fallback Backend at fallback-api.example.com:443, (3) A BackendTrafficPolicy with circuit breaking (maxConnections: 100, maxPendingRequests: 50) and retry logic (retry on 5xx and gateway-error, 3 attempts, exponential backoff from 500ms). Use the correct apiVersion and field names from Envoy Gateway v1.3.0.
